[advisories]
# quick-xml 0.39.4 remains only through wayland-scanner 0.31.10. That
# proc-macro parses protocol XML shipped by Wayland crates at build time; it
# does not parse user-controlled or runtime input. plist has been upgraded to
# 1.10.0 and now uses quick-xml 0.41.0. Remove these ignores when
# wayland-scanner publishes a release using quick-xml >= 0.41.0.
ignore = [
  "RUSTSEC-2026-0194",
  "RUSTSEC-2026-0195",
]
